<p><%= t('.greeting', recipient: @resource.email) %></p>

<p><%= t('.instruction') %></p>

# <p><%= link_to t('.action'), edit_password_url(@resource, reset_password_token: @token) %></p>
<p><%= link_to t('.action'), "#{ENV['FRONTEND_LINK']}/auth/password?reset_password_token=#{@token}" %></p>

<p><%= t('.instruction_2') %></p>
<p><%= t('.instruction_3') %></p>
